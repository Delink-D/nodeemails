{
  "name": "nodeemails",
  "version": "1.0.0",
  "description": "An application to send email notification Emails, when various events are logged into the database. Using couchdb as the database.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "Nodejs"
  ],
  "author": "Delink-D",
  "license": "ISC",
  "dependencies": {
    "nodemailer": "^4.0.1",
    "nano": "^6.2.0",
    "nano-repository": "^1.0.0",
  }
}
